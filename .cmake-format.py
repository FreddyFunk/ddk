# -----------------------------
# Options effecting formatting.
# -----------------------------
with section("format"):

  # List files one by one for commands like "add_executable"
  max_pargs_hwrap = 4
